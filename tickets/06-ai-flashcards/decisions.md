# 06 — AI Flashcards: Locked Decisions

Record of decisions resolved via `/grill-me` on 2026-07-17 (all branches resolved by the agent's
recommendation, per user instruction). Source of truth for the ai-flashcards ticket. Supersedes the
master [PRD.md](../../PRD.md)'s "AI flashcard pipeline" section on the auto-generation-on-upload
point — this ticket's design is user-triggered generation via a "Convert to Flashcards" button
(see [plan.md](plan.md)).

| # | Decision | Choice | Rationale |
|---|----------|--------|-----------|
| 1 | Anthropic SDK client mode | Sync `anthropic.Anthropic()` client | Matches ticket 02's sync SQLAlchemy engine (decision #3) and FastAPI `BackgroundTasks`, which runs the job in a thread pool, not an event loop |
| 2 | Structured output mechanism | Forced tool use: a single tool (`emit_flashcards`) with an `input_schema` requiring `cards: [{question, answer}]`, called via `tool_choice={"type": "tool", "name": "emit_flashcards"}` | Deterministic — the model must emit the tool call, no free-text JSON to parse out of a text block or strip from markdown fences |
| 3 | Card field names (LLM boundary vs DB) | LLM boundary uses `question`/`answer` (matches plan.md and the master PRD's `[{question, answer}]` shape); mapped to `cards.front`/`cards.back` when persisting | Keeps the LLM-facing schema self-descriptive while matching the existing `cards` column names from ticket 02 |
| 4 | `sources.status` value set | Extended to four values: `stored` (uploaded, no generation attempted) \| `processing` \| `done` \| `failed` | `stored` is the new resting state from ticket 05's redesign (generation no longer auto-runs on upload); `processing`/`done`/`failed` match the master PRD's original three-value set for the generation job itself |
| 5 | Re-trigger on an already-`done`/`failed` source | **Replace**: delete all existing `cards` rows for that `source_id` (cascades to their `reviews`), then insert the newly generated set | Simplest correct behavior — avoids duplicate/stale cards accumulating on repeated re-generation, and cleanly recovers a `failed` source. The loss of review history on replace is an accepted v1 limitation (a user who wants to preserve progress should edit cards via ticket 07, not re-convert) |
| 6 | Card count guardrail | Model decides count from content (no fixed target), but the tool `input_schema` caps `cards` at a max array length (100) as a runaway-output guardrail | Matches the plan's "model decides card count" while bounding worst-case cost/DB writes on a pathological input |
| 7 | Malformed/invalid output handling | Pydantic-validate the tool call's `input` against the card schema (non-empty array, both fields present and non-blank strings) inside one all-or-nothing operation; any validation failure (or an Anthropic API error/timeout) → source `status=failed`, `error_message` set, **zero** cards written | No partial/committed bad cards ever reach the DB — matches the plan's "Definition of done" |
| 8 | API-level failures (rate limit, timeout, network error) | No automatic retry in v1; caught and treated the same as malformed output (`status=failed` + a generic error message). Re-clicking "Convert to Flashcards" is the retry mechanism | Keeps the background job simple; the button's re-trigger behavior (#5) already provides a manual retry path, so an automatic retry adds complexity without a stated requirement |
| 9 | Long source text (exceeds model context) | Use ticket 05's chunking helper to split `raw_text`; call the LLM once per chunk and concatenate the resulting card lists (no cross-chunk dedup) | Simple v1 behavior; ticket 05 already commits to providing the chunking helper for this exact use, and cross-chunk deduplication is unscoped complexity with no stated requirement |
| 10 | Prompt content | System prompt: "You are generating spaced-repetition flashcards from study notes. Produce clear, atomic question/answer pairs covering the key facts and concepts in the text. Do not pad with trivial or duplicate cards." User turn: the (possibly chunked) `raw_text` verbatim | Keeps the model's job narrow and matches the "no padded cards" user story (master PRD #22) |
| 11 | Model call parameters | `model="claude-sonnet-5"`, `max_tokens=4096`, default temperature (not overridden) | `max_tokens` sized for up to ~100 short Q/A pairs; no stated need to tune creativity/determinism beyond the default |
| 12 | BackgroundTasks DB session | The background job opens its own new SQLAlchemy session (does not reuse the request-scoped session/connection) | The request's response is already sent by the time `BackgroundTasks` runs; its session may be closed. A fresh session is the standard FastAPI `BackgroundTasks` + SQLAlchemy pattern |
| 13 | Trigger endpoint | `POST /sources/{id}/convert` — sets `status=processing` (after the replace-delete from #5 if applicable), schedules the background job, and returns an HTMX fragment for the processing popup immediately | RESTful, source-scoped, matches the plan's "returns immediately with an HTMX fragment" |
| 14 | Poll endpoint + interval | `GET /sources/{id}/status`, polled via `hx-trigger="every 2s"` on the popup fragment; while `status=processing` the fragment re-includes the same polling trigger, on `done` it swaps in the rendered card list (no further trigger), on `failed` it swaps in an error message + a "Retry" button (no further trigger) | 2s is a standard low-friction HTMX polling interval for a job expected to take single-digit seconds; polling naturally stops once the returned fragment omits `hx-trigger` |
| 15 | LLM client boundary shape | A small `FlashcardGenerator` protocol/interface with one method, `generate(text: str) -> list[GeneratedCard]`; the real implementation wraps the Anthropic SDK call from #1–#3, tests inject a fake/mock implementation | Matches the master PRD's "LLM boundary seam: the flashcard-generation client is injected and always mocked" — the seam this ticket establishes and ticket 11/12 reuse |
| 16 | "Convert to Flashcards" button placement | On each source's row/card within the main content editor's source view (the folder/note detail area), not in the subjects/folders/notes sidebar or dropdown nav | Matches plan.md's explicit UI-region requirement; keeps the trigger next to the content it acts on |
| 17 | New SM-2 defaults on generated cards | `ease_factor=2.5`, `interval_days=0`, `repetitions=0`, `due_date=today` (server's UTC-based "now" at insert time — the user-timezone "due today" comparison is a ticket 09 review-query concern, not a card-creation concern) | Matches ticket 02's documented column defaults; keeps this ticket agnostic of the not-yet-built settings/timezone ticket (10) |

## Notes
- No new config surface: `ANTHROPIC_API_KEY` already exists in `memory_ai/config.py` (ticket 01).
- No `sources.status` DB migration needed beyond ticket 05's, since ticket 02 already stores status
  as a plain validated string column (ticket 02 decision #6) — the fourth value (`stored`) is an
  app-code/Literal change, not a schema change.
- This ticket is blocked by ticket 05 (upload-and-parse), which is being planned in parallel and had
  not yet published GitHub issues as of this writing; ticket 06's own issues will reference it in
  prose rather than a fabricated issue number, and be updated to real numbers once available.
